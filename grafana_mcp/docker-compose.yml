services:
  grafana-mcp:
    image: mcp/grafana 
    env_file:
      - ../.env
    ports:
      - "8002:8002"
    environment:
      - GRAFANA_URL=http://grafana:3000
      - GRAFANA_API_KEY=${GRAFANA_API_KEY}